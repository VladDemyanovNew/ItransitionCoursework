@using Microsoft.AspNetCore.Identity
@model AdminViewModel
@inject UserManager<IdentityUser> UserManager

<div class="text-center">
    <h1 class="display-4">Admin Page</h1>
</div>

@foreach (var mp in Model.Users)
{
    <a>@mp.UserName</a>
}